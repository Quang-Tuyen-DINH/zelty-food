import * as types from "./Constants";

const initialState = {
  products: []
};

const Reducer = (state = initialState, action: any) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default Reducer;